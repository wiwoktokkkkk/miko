<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Release hanya mempercayai CA sistem. Sertifikat CA yang dipasang user
         oleh aplikasi MITM seperti HTTP Canary tidak dipercaya. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug lokal tetap dapat memakai proxy pengembangan. Blok ini tidak
         diterapkan pada APK release. -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
